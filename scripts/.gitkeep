/* =========================
   1. Cambiar texto con input
========================= */
function cambiarTexto() {
    const textoUsuario = document.getElementById("inputTexto").value;
    const parrafo = document.getElementById("texto");

    if (textoUsuario.trim() !== "") {
        parrafo.textContent = textoUsuario;
    }
}

/* =========================
   2. Intercambio de imágenes al hacer clic
========================= */
const imagen = document.getElementById("imagen");
let imagenActual = 1;

imagen.addEventListener("click", () => {
    if (imagenActual === 1) {
        imagen.src = "images/imagen2.jpg";  // nombre exacto de la segunda imagen
        imagenActual = 2;
    } else {
        imagen.src = "images/imagen1.jpg";  // nombre exacto de la primera imagen
        imagenActual = 1;
    }
});

/* =========================
   3. Objeto en movimiento
========================= */
let posicion = 0;
let direccion = 1;

setInterval(() => {
    imagen.style.position = "relative";
    posicion += 3 * direccion;

    if (posicion > 200 || posicion < 0) {
        direccion *= -1;
    }

    imagen.style.left = posicion + "px";
}, 50);
